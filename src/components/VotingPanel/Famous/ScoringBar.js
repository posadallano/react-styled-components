import styled from 'styled-components';

const ScoringBar = styled.div`
    display: flex;
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    height: 50px;
    color: #fff;
`;

export default ScoringBar;
